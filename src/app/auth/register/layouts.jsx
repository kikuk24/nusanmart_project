export const metadata = {
  'title': 'Register',
  'description': 'Register'
}
export default function RegisterLayout({ children }) {
  return (
    <html>
      <body>
        {children}
      </body>
    </html>
  )
}